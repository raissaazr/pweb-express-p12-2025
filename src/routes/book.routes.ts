// src/routes/book.routes.ts
import { Router } from 'express';

const router = Router();

// Endpoint Anda akan ada di sini:
// router.get('/genre/:genre_id', ...);

// (Endpoint teman Anda nanti juga di sini, misal: router.post('/', ...))

export default router;